@page
@model ErrorModel
@{
    ViewData["Title"] = "Hata";
}

<h1 class="text-red-600">Hata</h1>
<h2 class="text-red-600">İsteğiniz işlenirken bir hata oluştu.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>İstek No:</strong> <code>@Model.RequestId</code>
        <br />
        <small class="text-gray-600">Destek için bu numarayı belirtebilirsiniz.</small>
    </p>
}

@if (Model.IsDevelopment)
{
    <h3>Geliştirme Modu</h3>
    <p>
        <strong>Development</strong> ortamında çalışıyorsunuz. Hata detayları geliştirici sayfasında görüntülenir.
    </p>
    <p>
        Canlı (production) ortamda <strong>ASPNETCORE_ENVIRONMENT</strong> değerini <strong>Development</strong> yapmayın;
        bu, hassas hata bilgilerinin kullanıcılara görünmesine yol açar.
    </p>
}
else
{
    <p>
        Lütfen daha sonra tekrar deneyin. Sorun devam ederse destek ile iletişime geçin.
    </p>
    <p>
        <a href="/" class="inline-flex gap-2 px-5 py-2.5 bg-primary hover:opacity-90 text-white rounded-lg font-medium transition">Ana Sayfaya Dön</a>
    </p>
}