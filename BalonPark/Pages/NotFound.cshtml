@page
@model NotFoundModel
@{
    ViewData["Title"] = "Sayfa Bulunamadı (404)";
    ViewData["Description"] = "Aradığınız sayfa mevcut değil veya taşınmış olabilir. Balon Park ana sayfasına dönebilirsiniz.";
}

<div class="max-w-2xl mx-auto py-12 sm:py-16 px-4 text-center">
    <h1 class="text-4xl sm:text-5xl font-bold text-ink mb-2">404</h1>
    <h2 class="text-xl sm:text-2xl font-semibold text-gray-700 mb-4">Sayfa Bulunamadı</h2>
    <p class="text-gray-600 mb-8">
        Aradığınız sayfa mevcut değil, kaldırılmış veya adresi değişmiş olabilir.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a asp-page="/Index" class="inline-flex items-center justify-center gap-2 px-6 py-3 rounded-lg bg-primary text-white font-medium hover:bg-primary-dark transition-colors" title="Ana Sayfaya Dön">Ana Sayfaya Dön</a>
        <a asp-page="/Iletisim" class="inline-flex items-center justify-center gap-2 px-6 py-3 rounded-lg border border-gray-200 text-ink font-medium hover:bg-gray-50 transition-colors" title="İletişim">İletişim</a>
    </div>
</div>
